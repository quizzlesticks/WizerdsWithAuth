const PortalMap = require('./portalmap.js');
const NexusMap = require('./nexusmap.js');

const MapFactory = {
    PortalMap: PortalMap,
    NexusMap: NexusMap
};

module.exports = MapFactory;
